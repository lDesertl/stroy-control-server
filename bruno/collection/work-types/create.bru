meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/work-types
  body: json
  auth: inherit
}

params:query {
  : 
}

body:json {
  {
    "name":"test"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
